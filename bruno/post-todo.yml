info:
  name: post-todo
  type: http

http:
  method: POST
  url: http://localhost:3000/api/todos
  body:
    type: json
    data: |-
      {
        "title": "english"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
